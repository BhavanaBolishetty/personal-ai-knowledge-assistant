# For local host-based dev (outside Docker): `pip install -r requirements.txt`
# still installs everything, unchanged. Split into two files so the
# Docker image (see Dockerfile) can skip requirements-local-embeddings.txt
# (and the torch install) in production, where EMBEDDING_PROVIDER=gemini
# means it's never used.
-r requirements-core.txt
-r requirements-local-embeddings.txt
